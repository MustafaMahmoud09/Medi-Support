plugins {
    id 'com.android.library'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace 'com.example.account.setting.mapper.execution'
    compileSdk compile_sdk

    defaultConfig {
        minSdk min_sdk
        targetSdk max_sdk

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        consumerProguardFiles "consumer-rules.pro"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = '17'
    }
}

dependencies {

    //project
    //dto declarations
    implementation(project(':features:account-setting:domain:dto-declarations'))

    //dto execution
    implementation(project(':features:account-setting:data:data-source:remote-data:dto-execution'))

    //shared entity execution
    implementation(project(':libraries:core:local-data:shared-entities:entity-execution'))

    //shared entity declarations
    implementation(project(':libraries:core:local-data:shared-entities:entity-declarations'))

    //domain model
    implementation(project(':features:account-setting:domain:domain-model'))

    //mapper declaration
    implementation(project(':features:account-setting:domain:mapper-declarations'))
}