plugins {
    id 'com.android.library'
    id 'org.jetbrains.kotlin.android'
    id("kotlin-kapt")
}

android {
    namespace 'com.example.libraries.core.local.data.database.creator'
    compileSdk compile_sdk

    defaultConfig {
        minSdk min_sdk
        targetSdk max_sdk

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        consumerProguardFiles "consumer-rules.pro"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = '17'
    }
}

dependencies {

    //project
    //reminder
    implementation(project(':features:reminder:data:data-source:local-data:entity-execution'))
    implementation(project(':features:reminder:data:data-source:local-data:data-access-object'))

    //article
    implementation(project(':features:article:data:data-source:local-data:entity-execution'))
    implementation(project(':features:article:data:data-source:local-data:data-access-object'))
    implementation(project(':features:article:domain:entity-declarations'))

    //blood pressure
    implementation(project(':features:blood-pressure:data:data-source:local-data:entity-execution'))
    implementation(project(':features:blood-pressure:data:data-source:local-data:data-access-object'))
    implementation(project(':features:blood-pressure:domain:entity-declarations'))

    //blood sugar
    implementation(project(':features:blood-sugar:data:data-source:local-data:entity-execution'))
    implementation(project(':features:blood-sugar:data:data-source:local-data:data-access-object'))
    implementation(project(':features:blood-sugar:domain:entity-declarations'))

    //heart rate
    implementation(project(':features:heart-rate:data:data-source:local-data:entity-execution'))
    implementation(project(':features:heart-rate:data:data-source:local-data:data-access-object'))
    implementation(project(':features:heart-rate:domain:entity-declarations'))

    //bmi
    implementation(project(':features:bmi:data:data-source:local-data:entity-execution'))
    implementation(project(':features:bmi:data:data-source:local-data:data-access-object'))
    implementation(project(':features:bmi:domain:entity-declarations'))

    //online booking
    implementation(project(':features:online-booking:data:data-source:local-data:entity-execution'))
    implementation(project(':features:online-booking:data:data-source:local-data:data-access-object'))
    implementation(project(':features:online-booking:domain:entity-declarations'))

    //offline booking
    implementation(project(':features:offline-booking:data:data-source:local-data:entity-execution'))
    implementation(project(':features:offline-booking:data:data-source:local-data:data-access-object'))
    implementation(project(':features:offline-booking:domain:entity-declarations'))

    //shared-entity
    implementation(project(':libraries:core:local-data:shared-entities:entity-execution'))

    implementation "androidx.room:room-runtime:$room_version"
    annotationProcessor "androidx.room:room-compiler:$room_version"
    //for kotlin flow and coroutine
    implementation "androidx.room:room-ktx:$room_version"

    // To use Kotlin annotation processing tool (kapt)
    kapt "androidx.room:room-compiler:$room_version"
//
//    // optional - RxJava2 support for Room
//    implementation "androidx.room:room-rxjava2:$room_version"
//
//    // optional - RxJava3 support for Room
//    implementation "androidx.room:room-rxjava3:$room_version"
//
//    // optional - Guava support for Room, including Optional and ListenableFuture
//    implementation "androidx.room:room-guava:$room_version"
//
//    // optional - Test helpers
//    testImplementation "androidx.room:room-testing:$room_version"
//
//    // optional - Paging 3 Integration
//    implementation "androidx.room:room-paging:$room_version"

}